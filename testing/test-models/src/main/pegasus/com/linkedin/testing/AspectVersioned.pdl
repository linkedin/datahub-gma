namespace com.linkedin.testing

import com.linkedin.metadata.aspect.BaseVersionedAspect

/**
 * For unit tests
 */
@gma.aspect.column.name = "aspectversioned"
record AspectVersioned includes BaseVersionedAspect{

  /**
   * For unit tests
   */
  value: string
}