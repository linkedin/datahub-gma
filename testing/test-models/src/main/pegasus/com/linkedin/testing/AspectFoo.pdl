namespace com.linkedin.testing

import com.linkedin.metadata.aspect.BaseVersionedAspect

/**
 * For unit tests
 */
@gma.aspect.column.name = "aspectfoo"
record AspectFoo includes BaseVersionedAspect{

  /**
   * For unit tests
   */
  value: string
}