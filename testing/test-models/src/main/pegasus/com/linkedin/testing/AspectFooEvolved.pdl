namespace com.linkedin.testing

import com.linkedin.metadata.aspect.BaseVersionedAspect

/**
 * For unit tests
 */
record AspectFooEvolved includes BaseVersionedAspect{

  /**
   * For unit tests
   */
  value: string

  /**
   * For unit tests
   */
  newValue: optional string
}